[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "LidarLounge"
authors = [{name = "Theo & Alix", email = "theo@lidarlounge.com"}]
dynamic = ["version", "description"]
